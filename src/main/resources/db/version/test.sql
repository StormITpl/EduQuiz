
-- create table if not exists users
-- (
--     id          uuid not null primary key,
--     nickname    varchar(255) not null,
--     createQuiz  varchar(255) not null
-- );
--
-- insert into users (id, nickname, createQuiz)
-- values (random_uuid(), 'Java', 'Quiz_1');